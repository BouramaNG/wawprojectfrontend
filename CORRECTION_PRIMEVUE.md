# 🔧 Correction PrimeVue

## Problème
Le package `@primevue/themes/aura` n'est pas trouvé.

## Solution
J'ai simplifié la configuration PrimeVue pour utiliser le thème par défaut (Material Design).

Si vous voulez utiliser le thème Aura, installez :
```bash
npm install @primevue/themes
```

Ou utilisez PrimeVue sans thème personnalisé (configuration actuelle).

---

**Le dashboard devrait maintenant fonctionner !** ✅

